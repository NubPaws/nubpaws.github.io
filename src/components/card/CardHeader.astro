---
import { cn } from '@/lib/utils';
import type { HTMLAttributes } from 'astro/types';

const { class: className = '', ...rest } = Astro.props as HTMLAttributes<'div'>;
---

<div class={cn('flex flex-col space-y-1.5 p-6', className)} {...rest}>
  <slot />
</div>
