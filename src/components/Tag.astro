---
import { cn } from '@/lib/utils';
import type { HTMLAttributes } from 'astro/types';

const { class: className = '' } = Astro.props as HTMLAttributes<'span'>;
---

<span class={cn('px-2 py-1 bg-blue-100 text-blue-800 text-sm rounded-full', className)}>
  <slot />
</span>
